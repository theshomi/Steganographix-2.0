const CryptoBox={async deriveKey(password,salt){const base=await crypto.subtle.importKey('raw',new TextEncoder().encode(password),'PBKDF2',false,['deriveKey']);return crypto.subtle.deriveKey({name:'PBKDF2',salt,iterations:100000,hash:'SHA-256'},base,{name:'AES-GCM',length:256},false,['encrypt','decrypt'])},random(n){const x=new Uint8Array(n);crypto.getRandomValues(x);return x},async encrypt(password,data){const salt=this.random(16),iv=this.random(12),key=await this.deriveKey(password,salt);const encrypted=new Uint8Array(await crypto.subtle.encrypt({name:'AES-GCM',iv},key,data));return {salt,iv,encrypted}},async decrypt(password,salt,iv,data){const key=await this.deriveKey(password,salt);return new Uint8Array(await crypto.subtle.decrypt({name:'AES-GCM',iv},key,data))}};
